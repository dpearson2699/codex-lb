## 1. Schema (WP-A)

- [x] 1.1 Add `subscription_overflow_source_id` and `subscription_overflow_drain_until` to `DashboardSettings` and the `ModelSourcePin` model with its `purge_at` index.
- [x] 1.2 Add Alembic revision `20260908_000000_add_subscription_overflow` on the current head with guarded, idempotent upgrade and full downgrade.
- [x] 1.3 Cover upgrade/downgrade, idempotent re-run against a partially applied schema, and PostgreSQL invalid-index repair in `tests/integration/test_migrations.py`.
- [x] 1.4 Run migration policy and drift checks on SQLite and PostgreSQL and strict OpenSpec validation.

## 2. Designation settings, preflight, drain deadline, dashboard (WP-B)

- [x] 2.1 Extend this change with the `model-source-routing` designation and preflight requirements.
- [x] 2.2 Settings API: tri-state `subscription_overflow_source_id`, eligibility validation (`400 subscription_overflow_source_invalid`), drain deadline arming/clearing in the same row update, audit `changed_fields`, cross-replica cache invalidation.
- [x] 2.3 `GET /api/settings/subscription-overflow/preflight` (write access; 404 for unknown sources; warnings never block).
- [x] 2.4 Deleting the designated model source clears the designation and arms the drain deadline in the delete's transaction, then invalidates the settings cache.
- [x] 2.5 Dashboard Routing card: designation select (Off sentinel), drain notice showing and gated on the derived pin expiry, inline preflight, help text, i18n en/ko/zh-CN, vitest coverage.
- [x] 2.6 `docs/routing.md` operator explainer linking back to `model-source-routing`.
- [x] 2.7 Inertness proof: request-path/core ratchet unit test plus the end-to-end test that an exhausted pool still answers `429 usage_limit_reached` with a source designated and never contacts it.

## 3. Pins, routing, observability, rollout (WP-C1, WP-C2, WP-D, WP-G)

- [x] 3.1 Extend this change with the pin repository, overflow routing, observability, and rollout requirements.

### 3.x WP-C1 pure modules and direct source-route hardening (no overflow wiring)

- [x] 3.2 `app/modules/proxy/source_admission.py`: per-source bulkhead enforcing `ModelSource.max_concurrency` (`NULL` unlimited), `SourceAdmission` claims released exactly once by whichever latch holds them (`release_if_unowned` route latch, `release(trial_result)` owner latch), `TrialClaim` hook for the WP-C2 breaker; direct routing claims before the API-key reservation and answers `503 model_source_busy` + `Retry-After: 1` with nothing owned.
- [x] 3.3 `app/modules/proxy/source_dispatch.py`: `SourceDispatch` single latch (`close_source -> settle_or_release -> release_claims + record_result -> write_row`, every step independently latched, failure-isolated, cancellation deferred), `abandon()` cancellation classes (`client_disconnected_during_open`, `source_stall_abandoned`, `client_disconnected_before_body`, `dispatch_interrupted`), `finalize_transport()`, `SourceStreamingResponse`, `open_with_disconnect_watch` (owned open task, 250 ms disconnect poll, 10 s stall evidence, child always cancelled and awaited), `settlement_stream` outermost body layer, settle-at-estimate policy, verified pin-write hook + synthesized `response.created`/`response.failed` pair (primitive only; never armed by direct routing).
- [x] 3.4 `app/modules/proxy/api.py`: `_source_responses_response` rewired onto the owner for limited and unlimited keys (live streaming; the Responses limited-key buffered branch is removed, chat completions keep theirs), telemetry stripped through `strip_source_telemetry`, raced open, non-stream disconnect check, source `Retry-After` merged into pre-open error headers, `context` threaded from both routes for the scheduler/clock/cleanup seams; api.py timing allowances unchanged (19/8).
- [x] 3.5 `app/core/metrics/prometheus.py`: `codex_lb_model_source_dispatch_total{kind,status}`, `_dispatch_abandoned_total{stage}`, `_timeout_total{phase}`, `_bulkhead_rejections_total{source_id}`, `_bulkhead_in_flight{source_id}`, `_usage_estimated_total{source_id,cause}`, `codex_lb_model_source_live_pins{kind}` (registered; sampler lands with WP-C2) in both the real and the stub branch.
- [x] 3.6 Tests: `tests/unit/test_source_admission.py`, `tests/unit/test_source_dispatch.py` (virtual-time watch, latch, settle figures incl. a hypothesis property, synthesized pair, transport finalizer), `tests/integration/test_model_source_dispatch.py` (real stub relay: live limited-key streaming, estimate on missing usage and post-item cancel, release before, non-stream `usage_unavailable`, abandonment cases a-e, `max_concurrency=1` busy, `Retry-After` passthrough, row attribution, telemetry stripped at the stub, static zero-hot-path ratchet); `api-keys` and `proxy-runtime-observability` deltas.
- [x] 3.7 Projection gate: `app/modules/model_sources/projection.py` (`strip_source_telemetry`, `overflow_portability_view`, closed `DeclineReason` set), `app/modules/proxy/replay_safety.py` (`responses_payload_is_provider_portable`, `transcript_is_source_free`, `is_binding_turn_state`; shared predicate hardened to fail closed on malformed nested values), `selection.py::select_overflow_model_source` (designated source only, no registry-precedence skip), `catalog.py::source_model_supports_vision`; synthetic gpt-5.5 / gpt-5.6 Lite fixtures; `responses-api-compat` delta; unit tables per decline reason, order cascade and hypothesis properties.
- [x] 3.8 Transport: `app/modules/model_sources/forwarding.py` (connect 10 s / header 20 s / first-frame 30 s deadlines, idle cap `min(stream_idle_timeout_seconds, 300)` on the scheduler seam, first-chunk open, frame classification and holder observations, pre-content hook with oversized frames counted as content, `retry_after` / `timeout_phase`, Responses-only 401/403 → `502 model_source_credentials_error`, `SourceStreamTransport` close latch), `app/core/clients/http.py` dedicated model-source session (`lease_model_source_session`), `tests/integration/model_source_helpers.py`; `outbound-http-clients` delta; virtual-time and real-socket deadline suites.
- [x] 3.9 Pins and retention: `app/modules/proxy/model_source_pins.py` (thread-only / anchor / bounce keys, drain-capped expiry, dialect repository, positive-only `PinCache`, `lookup_pin_bounded`, `PinWriteExecutor` verified outcomes, `delete_durably`), `app/core/retention/job.py::prune_model_source_pins` + scheduler tick regardless of opt-in, inertness ratchet relaxed for the two pin readers; `model-source-routing` and `data-retention` deltas; SQLite/PostgreSQL parity, drain-cap property, N=50 writer storm.
- [x] 3.10 Exhaustion probe: `app/modules/proxy/_load_balancer/exhaustion_probe.py::probe_pool_usage_exhaustion` (read-only `check_opportunistic_admission(lease_kind=None, service_tier=, observe_only=True)`), `_load_balancer/opportunistic_admission.py` extraction with `apply_lease_release` / `detached_runtime_snapshot`, `_service/support.py::opportunistic_admission_account_scope`; `account-routing` delta; selector-parity hypothesis property (pre-existing drain-strategy divergence pinned as a strict xfail), runtime byte-identity and `service_tier` flip integration tests.
- [x] 3.11 Integration: merge the five packages and `origin/main` into `feat/2123-wp-c1-overflow-core`, count oversized frames as content for the pre-content hook, record decisions 21–28, and run the full gate set (ruff, ty, proxy architecture / cancellation safety / timing seams, strict OpenSpec validation, single Alembic head) with the new, model-source, proxy-responses, api-keys and simulation suites green.
- [x] 3.12 Review fixes (decisions 29–34): admission estimate under the route-helper latch (bulkhead slot never leaks on a raising estimate); EOF tail parsed as a frame before the withheld bytes are released and an aggregate withheld-bytes cap that fails closed; clean EOF without a terminal recorded as `error model_source_stream_truncated` and released while a relayed oversized success terminal stays a success; header / first-frame deadlines armed for Responses streams only (chat streams keep the total-budget-only open); observability delta names the registered `codex_lb_model_source_live_pins` gauge with a two-way drift guard; account-routing parity scenario narrowed to the proven statements with the production-knob property outside the drain strategies.
- [x] 3.13 Review fixes (round 2, decisions 35–38): `settlement_stream`'s cancel handler releases and records `error model_source_response_failed` when a client leaves right after a relayed failure terminal (before the source's EOF) instead of charging the cancel estimate; the stream usage parser joins multi-line `data:` fields through the shared `extract_sse_data` so it and the public wrapper never disagree inside one pipeline; `_source_client_timeout` arms only `sock_connect` (not `connect`, which in aiohttp also bounds the pooled-connection wait) so a saturated dedicated pool queues for a slot instead of failing fast as `model_source_unreachable`, with the `outbound-http-clients` delta stating the pool-wait boundary and a queue-not-unreachable scenario; and behavioural assertions cover the four metric-increment clauses (`…dispatch_total{kind,status}`, `…dispatch_abandoned_total{stage}`, `…bulkhead_rejections_total{source_id}`, `…usage_estimated_total{cause}`) that the `_inc` helper would otherwise let drift silently.
- [x] 3.14 Review fixes (round 4, decisions 39–42): the stream usage parser ignores one leading UTF-8 BOM like the event-block reassembler (BOM-prefixed data-only terminals run the pre-content hook and surface their usage); `settlement_stream`'s completion branch follows the client-visible terminal so a success terminal the public wrapper rewrote into `response.failed` is `error model_source_response_invalid` and released; the cancel estimate keys on `content_delivered` (set at the body's flush point) instead of the parser's output item — delivered delta-only answers are charged, items still withheld ahead of an in-flight pin write are not — and a disconnect right after a relayed success terminal is a `success`; and `probe_pool_usage_exhaustion` takes the caller's settings snapshot and declines under the drain strategies (`codex_lb_pool_exhaustion_probe_declined_total{reason="drain_strategy"}` + reason log), replacing the strict xfail with positive decline tests and a premise test.
- [x] 3.15 Review fixes (round 5, decision 43): delivery is decided by `settlement_stream` from the event frames it hands to the transport (`relayed_frame_delivers_content`, the parser's own `classify_responses_frame`) instead of the body's flush point, which sits below the public wrapper that drops vendor events under the SDK contract, parks `response.*` frames ahead of `response.created` and holds reasoning deltas; the owner no longer mirrors `SourceUsageHolder.content_delivered` (kept as the hook-ordering evidence), so a limited key that leaves after only bookkeeping frames reached the wire is released (api-keys requirement sentence + two scenarios: dropped vendor event, delta parked ahead of `response.created`).
- [x] 3.16 Review fixes (round 6, decisions 44–48): the chat-completions stream open returns at the source's headers as on `main` (the body reads the first token under the total budget alone, so a client that leaves during prompt processing frees the connection, the lease and the reservation; route-level real-disconnect test, ASGI harness shared through `tests/integration/model_source_helpers.py`); every source route relays the source `Retry-After` (`_source_error_response_headers` in the four handlers that answer a pre-open source error -- chat stream open, chat non-stream, embeddings, transcription -- one route-level test per family; the buffered limited-key chat handler only consumes a started body and never observes one, decision 54); the stream usage parser carries a trailing CR across chunks so a CRLF split at a chunk boundary is one line ending; a typeless `{"error": {...}}` record is the `error` failure terminal to the parser and to `_relayed_event_type` through the wrapper's own `classify_event_type`; and `settlement_stream` latches the client-visible terminal (failure sticky) instead of reading the last relayed frame. outbound-http-clients and api-keys deltas updated with the sentences and scenarios.
- [x] 3.17 Review fixes (round 7, decisions 49–50): `_relayed_event_type` reads the `event:` line at the SSE boundary set and a raw block's type through `parse_sse_data_json`, so bare-CR framed and multi-line `data:` frames classify as the client parses them (unit table rows; route-level limited-key cancel after CR-framed `created`/`in_progress` on both Responses paths releases); an empty chat-completions `2xx` stream ends the started body cleanly (`main` parity, no exception out of the ASGI body) while the unlimited row records `error invalid_upstream_response` and the limited-key buffered handler answers `502 invalid_upstream_response` before any byte (unit + two route tests). api-keys and outbound-http-clients deltas updated.
- [x] 3.18 Follow-ups to #2208 (decision 51): the direct chat-completions stream owns its transport (`SourceChatStream.transport`/`aclose()` shared with `SourceResponsesStream`; `SourceStreamingResponse` takes any `TransportOwner`; `SourceChatStreamOwner` closes a never-started body's transport, releases the reservation and writes the `cancelled client_disconnected_before_body` row), with a route-level pre-body abandonment test on the ASGI harness (`stall_response_start`) and unit coverage of the owner latch; outbound-http-clients delta sentence and scenario.
- [x] 3.19 Follow-ups to #2208 (decision 52): `strip_source_telemetry` removes `client_metadata` and `access_programs` whole and only the Codex `reasoning_summary_delivery` key from `stream_options` (object dropped when emptied; `include_obfuscation` and non-object values forwarded); responses-api-compat delta requirement and scenarios, unit table incl. the overflow view declining a surviving `stream_options`.
- [x] 3.20 Follow-ups to #2208 (decision 53): `SourceStreamUsageParser._observe_responses_event` classifies the parser's own parsed frame without a per-frame `dict(event)` copy (micro-benchmark in the decision).
- [x] 3.21 Follow-ups to #2208 (decision 54): the buffered limited-key chat handler's dead `Retry-After` merge is removed with the post-open reasoning at the call site (behaviour unchanged); `test_body_phase_errors_carry_no_retry_after` pins that body-phase forwarding errors carry no `retry_after`; 3.16 / decision 45 wording corrected to four families.

### 3.x WP-C2 overflow wiring with WP-D folded in (owner decision 2026-09-09)

- [x] 3.22 `app/modules/proxy/overflow.py` decision module: `resolve_subscription_overflow` in the §4.2 order (settings fast path -> thread pin -> anchor -> drain check -> O(1) declines -> probe with the step-1 snapshot -> designated-source resolution -> strip + view + verdict -> claims last), `dispatch_pinned` (503/400 split, neutral release with the durable delete first, tombstones, model switch, unsupported input), `dispatch_anchor` (also in drain mode), `compact_pin_denial`, `handshake_denial`, `apply_usage_limit_hint` (promo header native / sentence SDK, `not_portable_history` only), `background_job_allowed` (allowlist + memgen refusal), `SourceBreaker` (3 / 30 s / leased half-open trial, token in every non-open state), `try_claim_overflow`, `FastDeclineSet`, `OverflowPinExecutor`, `PinToucher`, `record_overflow_transport_decision`; virtual-time unit suites (decision table, breaker, executor, spy for the NULL fast path, `CancelledError` holds nothing).
- [x] 3.23 C1 interface patches: `try_claim(trial=)`, `SourceDispatch.on_finished` + `pin_intent.resolve(holder.response_id)` at the content trigger, `PinIntent.resolve`/anchor fields, `get_pin_cache`, `replay_safety.strip_input_item_ids`; `codex_lb_subscription_overflow_total{route,outcome}` and `codex_lb_model_source_breaker_state{source_id}` registered in both prometheus branches; live-pin gauge sampled in `run_retention_pass` (leader tick); `tests/unit/test_metrics.py` drift regex extended to the overflow counter.
- [x] 3.24 `app/modules/proxy/api.py` insertion: decision call in `responses()` and `v1_responses()` (never `internal_bridge_responses`), `_overflow_source_response` with the route-helper latch, `_source_responses_response(overflow=)` (claims transfer, owner kwargs, `strip_service_tier`), non-stream pin commit, `compact_pin_denial` in `_compact_responses`, `handshake_denial` beside the WebSocket transport denial, the 429 hint in `_logged_error_json_response`; timing allowances 19/8 unchanged. Tests: positive ratchet replacing `test_subscription_overflow_inert.py`, route suite, golden byte-identity (429, native/SDK stream, non-stream) by stub equivalence, NULL-column spy at route level, decline goldens, source-direction header capture on the overflow and direct paths plus the `forwarding.py` static ratchet.
- [x] 3.25 WebSocket parity (WP-D folded): `app/modules/proxy/_service/websocket/overflow.py` (`bounce_exhausted_websocket_turn` with the 60 s bounce row and the wrapped 503 `model_source_requires_http_transport` event carrying top-level `status`; `bounce_pinned_or_anchored_websocket_turn`), two mixin call sites (<= 8 lines), handshake 426 on live pin / tombstone / bounce / lookup timeout with the capability exemption; unit + integration suites incl. the `server_is_overloaded`/`slow_down` mutant and the bounce-then-426 chain.
- [x] 3.26 OpenSpec deltas for the shipped behaviour: `model-source-routing` (designation and pin requirements reworded from inert to shipped + ship-dark guarantee and routing coupling; admission decision, pinned/anchored routing, breaker + claims, attribution, source-direction headers, ship-dark rollout and canary), `responses-api-compat` (one lifecycle + codes, byte-identical declines + hint, WebSocket 426/in-band bounce, pinned compaction 400), `account-routing` (trigger = probe answer, Q4 reversal), `proxy-runtime-observability` (counter enums, breaker gauge, live-pin cadence, transport-decision policy, `kind` values, log names); decisions 55-70; proposal updated; strict validation green; `tests/unit/test_subscription_overflow_spec_delta.py` pins the delta names to the interface contract.
- [x] 3.27 `docs/routing.md`: staged notice replaced by the shipped / ship-dark status and the production flip gate, WebSocket behaviour (426 on evidence, in-band bounce, Codex >= 0.99.0), the history hint, source-direction headers, observability names, "Canary and drills"; dashboard `stagedNotice` paragraph and its en/ko/zh-CN keys removed together (chip, `source` filter and cost tile shipped in 4.1-4.3 below).
- [x] 3.28 Integration: merge the packages and `origin/main` into `feat/2123-wp-c2-d-overflow-wiring`, tick 3.22-3.25, run the full gate set (ruff, ty, proxy architecture with unchanged ceilings, cancellation safety, timing seams 19/8, settings tiers, strict OpenSpec validation, single Alembic head, vitest + tsc) with the new, model-source, proxy-responses, WebSocket, api-keys and metrics suites green; PR description states that WP-D was folded by owner decision.
- [x] 3.29 Review fix (decision 72): the decision runs before a direct source dispatch on both routes (`direct_source=` handed to `resolve_subscription_overflow`; the service-tier fallback stays on the subscription path), so a live pin or anchor beats a model another source serves directly and a directly owned model without evidence returns to direct routing after the lookups without probing or counting; unit table, wiring test with the decision double, route-level pin-vs-direct test (ciphertext 400 with neither source contacted; source-free neutral release then the direct source serves the id-stripped body); `model-source-routing` delta and `docs/routing.md` updated.
- [x] 3.30 Review fix (decision 73): pinned and anchored dispatches refuse an `input_image` the source model cannot see (`replay_safety.input_carries_image_parts`, the vision step alone) with `400 subscription_overflow_unsupported_input` in its image variant, pin kept, nothing claimed; declaring `supports_vision` restores the dispatch; unit (pinned, anchored, with vision) and route-level tests (refused, then served after the operator declares vision); `model-source-routing` scenario extended, `docs/routing.md` updated.
- [x] 3.31 Review fix (decision 74): the half-open breaker trial closes at the dispatch's first output item (`observe_first_output_item` through `SourceDispatch` -> `SourceAdmission` -> `BreakerToken`, one attribute read per chunk in `settlement_stream` until the item; the slot stays held until `finish()`, a later counted failure still counts as a closed-state failure); breaker, admission and settlement-stream tests; `model-source-routing` breaker requirement and scenario extended.
- [x] 3.32 Review fix (decision 75): `stream_trial_result` counts a mid-stream forwarding error, an exception out of the body and a clean EOF without a terminal as breaker failures after the first output item as well; failure terminals after the item stay uncounted; table + settlement-stream tests; `model-source-routing` breaker requirement and scenarios.
- [x] 3.33 Review fix (decision 76): `OverflowFinishedHook(route)` replaces the bare transport-decision hook and counts `pin_commit_failed` / `pin_commit_unverified` once per dispatch from the owner's `pin_outcome`; unit (hook, streaming and non-streaming lifecycles) and route-level assertions on the counter (wiring double and real decision).
- [x] 3.34 Review fix (decision 77): `bounce_pinned_or_anchored_websocket_turn` guards its pre-registration awaits and disposes the prepared turn on cancellation (`_dispose_prepared_turn`: idempotent release + `cancelled` `stream_incomplete` row, cancellation deferred; emitter outside the guard); helper-level and session-level cancellation tests plus the not-disposed-twice pin; `responses-api-compat` requirement sentence and scenario.
- [x] 3.35 Pre-flip follow-up (decision 78): an owed anchor that no source `response.id` can build fails the pin write closed -- `PinIntent.anchor_resolved`/`anchor_pending` plus an idempotent `resolve`, and `PinWriteExecutor.commit` returns `not_written` (`reason=anchor_unresolved`) with no statement issued before the empty-write short-circuit, so the thread pin is not written either and both the streaming hook and the non-stream finish inherit the refusal without a route change; `store: false` intents keep their thread-pin-only write and a pinned/anchored continuation stays vacuously `written`; unit tests at the executor and the dispatch hook plus route-level regressions (keyless SDK stream, anchored continuation, non-stream 503, native `store: false` over-reach guard); `model-source-routing` pin-write and pinned requirements, `responses-api-compat` clause, `docs/routing.md` residual and the canary drill check.
- [x] 3.36 Pre-flip drills made executable: `tests/integration/test_subscription_overflow_canary_drills.py` (marker `overflow_drill`, entry point `make test-overflow-drills` / `pytest -m overflow_drill tests/integration`) rehearses all seven `docs/routing.md` drill rows -- one `test_drill_*` per row asserting that row's own outcomes: the wire answer and its verbatim message every time, and beyond that whichever of the request-log rows, the pin rows, the `subscription_overflow_total{outcome}` label, the `ApiKeyUsageReservation` outcome, the bulkhead and the breaker state the row promises (no drill asserts all six -- a keyless drill has no reservation, a drill that fails before a decision pins the abandonment stage or timeout phase instead of an outcome label, and only Stall is a row about the breaker; the two clauses of the Kill switches row its own drill does not cover -- the cross-replica settings-cache window and the release half -- are named in `docs/routing.md`, the first as manual and the second as the Neutral release drill's on a disabled source) -- plus the two behaviours the table never stated (a mid-stream idle cut truncates without a terminal; the ship-dark fast path returns at `drain_until`, not when the designation goes `NULL`). Deadlines are shortened by re-binding `_open_source_stream`'s default parameters, so the client-visible text still names the production 20 s / 30 s; the calendar drill constructs a past clear time instead of waiting. Corrected contract: a black-holed address has two shapes -- a dropped SYN fails the connect phase with `502 model_source_unreachable` (the prose promised `504 model_source_timeout`, which that setup cannot produce), and only a source that accepts TCP and stays silent yields the `504`; fixed in `docs/routing.md` and in both the requirement and the "Drill outcomes" scenario. `docs/routing.md` also gains the Rehearsal column, the one-command entry point with what it proves and what it does not, and the per-drill manual-only residue; `tests/unit/test_subscription_overflow_spec_delta.py` guards the row <-> test bijection and the entry point. No production file is touched.
- [x] 3.37 Review fix: two drill-row clauses that had no rehearsal now have one. The Kill switches drill asserts what "pinned conversations drain" means -- the drained turn's `subscription_overflow_pinned` request-log row, the `dispatched_pinned` outcome, and the thread pin plus the anchor the turn minted, still the source's and still inside `PIN_IDLE_TTL` -- and, on the disable/delete legs, that refusing a conversation is not releasing it (pin unchanged, no request-log row); the Clear-then-touch drill runs the drain deadline, re-writing the rows the window produced an hour before a window that has just closed (the state the CL-3 cap guarantees) and asserting that the real retention pass, with retention disabled as it is by default, deletes exactly them and raises no `model_source_pins_drain_invariant_violated`. The claim in 3.36 and in `docs/routing.md` that every drill asserts the whole observable union was false in both documents and is replaced by the per-row rule plus the named manual residue.
- [x] 3.38 Review fix (round 2): the drill contract is per **clause**, not per row, and the mapping is machine-checked. `docs/routing.md` splits every **Expected** cell into `;`-separated clauses, each either asserted by a rehearsal the row names or listed under the new "Not rehearsed -- verify manually during the canary" heading with the `*Observe:*` sentence that settles it (live ChatGPT traffic through a stall, the cross-replica settings-cache window, whether the real source mints `response.id` on `response.created`); the old summary claim that a row's drill asserts the row's whole union, and the hand-written exception list that went stale beside it, are gone. Rehearsals close the clauses that had none: the Kill switches drill releases a source-free conversation off a *deleted* source (`pinned_released_neutral`, the pin gone, the row carrying an account) as well as refusing a reasoning-bearing one, the accepting half of Anchor timing is a `test_drill_*` the row names and reads the created-before-content ordering off the client stream, the ciphertext refusal is named too and asserts the surviving pin, and Stall pins `Retry-After: 2` as the literal the runbook prints plus "no `200` preceded" on both legs. `tests/unit/test_overflow_drill_coverage.py` holds the clause -> assertion map and fails when a row's clauses no longer reconstruct its **Expected** cell (a promise added), when a named assertion has left the drill (coverage deleted), when a row has no clause at all (a row added), when a manual clause loses its entry, its row or its observation, or when the section's own count drifts; its failure modes are exercised against mutated copies of the doc and the suite, as `test_request_log_source_parity.py` does for its parity function. The row <-> rehearsal bijection moves out of `test_subscription_overflow_spec_delta.py` into that module, which owns the only parser for the table. No production file is touched.
- [x] 3.39 Review fix (round 3): the coverage guard could not see a drill that was switched off. It matched assertion snippets against `ast.get_source_segment` of the `FunctionDef`, which excludes decorators, so `@pytest.mark.skip` above a drill left every clause of its row "covered" while the marker-selected run reported it `skipped` -- and an early `pytest.skip()` or a leading `return` did the same from inside, both being executable and therefore untouched by the comment/string blanking. `_drills` now reads each rehearsal's marks (its own, its parametrised cases' and the module's `pytestmark`) and its first executable statement, and reports every clause of a quarantined drill as unrehearsed, naming the drill and why it does not run; a drill that loses the `overflow_drill` marker is reported too, because that marker -- not the path -- is what the documented command selects. `_coverage_errors` also fails when it has nothing to check (no table, no drill, no rehearsal in the map), so a parser that stops working cannot read as a pass. The two prose claims that outran the code (`docs/routing.md` "still *running*", the module docstring's "and still runs") now describe exactly this, and both name the one residue source cannot see -- a drill pytest never collects at all (a `conftest` ignore, an `addopts` deselection, a fixture that skips at setup), which the run itself reports and the runbook now tells the operator to read. Live collection in a subprocess was considered and rejected: `--collect-only -m overflow_drill tests/integration` costs ~13 s and would fail this unit guard for any unrelated integration-collection error. Local `codex review` then closed two remaining false negatives in the same check: `pytestmark` is read from the annotated and appended forms as well as the plain assignment, and the escape calls are resolved through the module's own import aliases (`import pytest as pt`, `from pytest import skip as stop`) instead of the spelling `pytest.skip`. No production file is touched.

## 4. WP-G request-log source attribution UI and the overflow tile

- [x] 4.1 `source` chip in the recent-requests **account** cell (fresh vs pinned; no new request-log column, so stored column preferences keep their meaning and non-overflow rows are untouched) plus a detail-dialog source / model-source-id / model-source-kind block; `frontend/src/features/dashboard/request-log-source.ts` as the single closed exact-match mapping shared by the chip, the filter options and the tests; en/ko/zh-CN keys.
- [x] 4.2 `source` filter: repeated `?source=` query params through `api.py` -> `service.py` -> `repository.py` (`RequestLog.source.in_(...)` beside the api-key predicate), the demand-rollup fast path disabled while it is active and the count-cache key extended; static two-option control rendered only when the overview reports overflow activity, or when a URL-carried selection must stay visible and clearable; `/options` deliberately unchanged (no fifth DISTINCT pass for a two-value domain).
- [x] 4.3 Overflow tile: additive `summary.subscriptionOverflow` on `GET /api/dashboard/overview` (`requests`, `costUsd`, `usageLessRequests`, `livePins`; absent when the install has never dispatched an overflow request and holds no live pin), aggregate in `app/modules/settings/subscription_overflow.py` over `source IN (...)` on `idx_logs_source_requested_at` plus an all-source live thread-pin count, sharing the activity aggregate's window bounds; `DashboardStat` appended by `buildDashboardView` with a neutral empty state, no sparkline, and a separate count for rows whose source reported no usage. Declines stay Prometheus-only: `/metrics` has no dashboard read surface.
- [x] 4.4 Positive ratchet extended: `app/modules/dashboard/repository.py` joins `_DASHBOARD_READERS` (the one new importer of the settings helper) and the three overview pass-through files join a narrower `_OVERVIEW_FIELD_READERS` that grants only the designation identifier; `tests/unit/test_subscription_overflow_dashboard_summary.py` pins the two source literals to `overflow.REQUEST_LOG_SOURCE_*` and fails the statement on a `LIKE`.
- [x] 4.5 `frontend-architecture` (ADDED) and `query-caching` (MODIFIED: `source` added to the non-expressible set) deltas, `model-source-routing` attribution sentence corrected from the prefix `LIKE` to the closed `IN` set, `docs/routing.md` deferral sentence replaced by the shipped behaviour and its caveats; vitest (mapping table, chip, filter control, hook URL round-trip, tile presence/absence, schema parity) plus pytest (source-filter listing, demand-rollup statement spy, API round-trip, `/options` unchanged, overview tile matrix, drift guards).
- [x] 4.6 Landing fixes: the overview's overflow read is gated on the two ship-dark settings columns (`never_designated`), so an install that never designated a source issues zero statements for the tile -- `get_overview` hoists the settings row it already loads and passes it down, and `tests/integration/test_dashboard_overview.py` counts the statements of a default-install poll against a designated one (delta exactly the three the tile costs). `ix_model_source_pins_kind_expires_at` (`kind`, `expires_at`) gives the live thread-pin count an index instead of a table walk: ORM metadata, one index-only revision following the repo's concurrent-create / invalid-leftover-repair convention, and an upgrade / downgrade / query-plan test in `tests/integration/test_migrations.py`.
